# Core dependencies
python-dotenv
watchdog
requests
pandas
openpyxl
Pillow

# SQLite support
sqlite-utils

# Airflow core (pin to a stable version)
apache-airflow==2.9.1

# Optional: If you use OCR.Space via requests
certifi
